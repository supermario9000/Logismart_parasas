<?xml version="1.0" encoding="UTF-8"?>
<Wix xmlns="http://schemas.microsoft.com/wix/2006/wi"
     xmlns:ui="http://schemas.microsoft.com/wix/UIExtension">

  <Product
    Id="*"
    Name="Logismart Parašo Generatorius"
    Language="1033"
    Version="1.0.0.0"
    Manufacturer="Logismart"
    UpgradeCode="0E3F1257-1F0B-44E5-9D6C-91E13EF18E9F"
    Codepage="65001">

    <Package InstallerVersion="500" Compressed="yes" InstallScope="perMachine" />

    <MajorUpgrade DowngradeErrorMessage="A newer version is already installed." />
    
    <MediaTemplate EmbedCab="yes" />

    <!--
    CHANGE 1: Switch from the minimal UI to a full-featured UI
              that includes a license agreement dialog.
    -->
    <UIRef Id="WixUI_Mondo" />

    <!--
    CHANGE 2: Add this variable. It tells the WixUIExtension where to find
              your custom license file. The Id "WixUILicenseRtf" is a
              special, required Id.
    -->
    <WixVariable Id="WixUILicenseRtf" Value="License.rtf" />

    <Feature Id="ProductFeature" Title="Main Feature" Level="1">
      <ComponentGroupRef Id="ProductComponents" />
    </Feature>
    
    <!-- This part for launching the folder after install remains the same. -->
    <Property Id="WIXUI_EXITDIALOGOPTIONALCHECKBOXTEXT" Value="Open installation folder" />
    <Property Id="WixShellExecTarget" Value="[INSTALLFOLDER]" />
    <CustomAction Id="LaunchFolder"
                  BinaryKey="WixCA"
                  DllEntry="WixShellExec"
                  Impersonate="yes"
                  Execute="immediate" />
    <UI>
      <Publish Dialog="ExitDialog"
               Control="Finish"
               Event="DoAction"
               Value="LaunchFolder">WIXUI_EXITDIALOGOPTIONALCHECKBOX = 1 AND NOT Installed</Publish>
    </UI>

  </Product>

  <!-- The rest of your file remains unchanged -->
  <Fragment>
    <Directory Id="TARGETDIR" Name="SourceDir">
      <Directory Id="ProgramFilesFolder">
        <Directory Id="INSTALLFOLDER" Name="LogismartGeneratorius" />
      </Directory>
    </Directory>
  </Fragment>

  <Fragment>
    <ComponentGroup Id="ProductComponents" Directory="INSTALLFOLDER">
      <Component Id="MainExecutable" Guid="1D0A724A-80F5-4D65-8CB9-676F7D0C9676">
        <File Source="Logismart_paraso_generatorius.exe" KeyPath="yes" />
      </Component>
    </ComponentGroup>
  </Fragment>
</Wix>